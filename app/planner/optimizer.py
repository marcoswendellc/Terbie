from app.planner.models import ExecutionPlan


class PlanOptimizer:
    """Placeholder optimizer for future planner improvements."""

    def optimize(self, plan: ExecutionPlan) -> ExecutionPlan:
        return plan
